//import static org.junit.jupiter.api.Assertions.*;

import org.junit.*;

class TestAbstraction {

	@Test
	void testShape() {
		Shape rectangle = new Rectangle();
		String output = rectangle.draw();
		Assert.assertEquals("Drawing Rectangle", rectangle.draw());
		
	}

	Drawing Rectangle

}
